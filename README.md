# Simple Python Game with PYGAME Community edtion

This is a basic python game. 
All you need to do is 

configure your venv and install requirements.txt
```bash
    python3 -m venv venv
```

```bash
  pip3 install -r requiremts.txt
```

Run the game with
```bash
    python3 alien_invasion.py
```
